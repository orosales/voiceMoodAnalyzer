# VoiceMoodAnalyzer Environment Configuration
# Copy this file to .env and fill in your actual values
# IMPORTANT: Never commit .env to version control!

# ============================================================================
# DATABASE CONFIGURATION
# ============================================================================

# PostgreSQL database name
POSTGRES_DB=mito_books

# PostgreSQL username
POSTGRES_USER=postgres

# PostgreSQL password - CHANGE THIS IN PRODUCTION!
# For production, use a strong password (16+ characters, mix of letters/numbers/symbols)
POSTGRES_PASSWORD=changeme123

# Database host (use 'postgres' for Docker, 'localhost' for local development)
POSTGRES_HOST=postgres

# Database port (5432 is PostgreSQL default)
POSTGRES_PORT=5432

# ============================================================================
# API KEYS
# ============================================================================

# OpenAI API Key - REQUIRED for Whisper transcription
# Get your key from: https://platform.openai.com/api-keys
OPENAI_API_KEY=sk-proj-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# ============================================================================
# APPLICATION CONFIGURATION
# ============================================================================

# Environment: development, staging, or production
ENVIRONMENT=production

# Backend port (default: 8000)
BACKEND_PORT=8000

# Frontend port (default: 80)
FRONTEND_PORT=80

# ============================================================================
# DOCKER HUB (For CI/CD only - not needed for local development)
# ============================================================================

# Docker Hub username (for pushing images via GitHub Actions)
DOCKER_USERNAME=your-dockerhub-username

# Docker Hub password or access token (store in GitHub Secrets, not here!)
# DOCKER_PASSWORD=<store-in-github-secrets>

# Docker image tag (default: latest)
IMAGE_TAG=latest

# ============================================================================
# DEPLOYMENT CONFIGURATION (For CI/CD only)
# ============================================================================

# OCI instance public IP (for SSH deployment via GitHub Actions)
# OCI_HOST=<store-in-github-secrets>

# OCI SSH private key (for deployment via GitHub Actions)
# OCI_SSH_PRIVATE_KEY=<store-in-github-secrets>

# ============================================================================
# NOTES
# ============================================================================

# 1. Required for basic operation:
#    - OPENAI_API_KEY (get from OpenAI platform)
#    - POSTGRES_PASSWORD (change default)

# 2. For GitHub Actions CI/CD, add these to GitHub Secrets:
#    - DOCKER_USERNAME
#    - DOCKER_PASSWORD
#    - OCI_SSH_PRIVATE_KEY
#    - OCI_HOST
#    - OPENAI_API_KEY
#    - POSTGRES_PASSWORD

# 3. Default passwords:
#    - Development: changeme123
#    - Production: Generate strong password (e.g., using: openssl rand -base64 32)

# 4. ML Models:
#    - Backend downloads ~2GB of models on first run (or pre-baked in Docker image)
#    - Models cached in /app/.cache directory
